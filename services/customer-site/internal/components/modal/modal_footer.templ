package modal

import "github.com/fedotovmax/microservices-shop/customer-site/pkg/utils"

type ModalFooterProps struct {
	Class string
}

templ ModalFooter(props *ModalFooterProps) {
	<div class={ utils.TwMerge("flex flex-col-reverse justify-between gap-2 border-t border-outline bg-surface-alt/60 p-4 dark:border-outline-dark dark:bg-surface-dark/20 sm:flex-row sm:items-center md:justify-end", props.Class) }>
		{ children... }
	</div>
}
