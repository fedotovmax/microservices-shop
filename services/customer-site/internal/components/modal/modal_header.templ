package modal

import "github.com/fedotovmax/microservices-shop/customer-site/pkg/utils"

type ModalHeaderProps struct {
	Class string
}

templ ModalHeader(props *ModalHeaderProps) {
	<div class={ utils.Cn("flex items-center justify-between border-b border-outline bg-surface-alt/60 p-4 dark:border-outline-dark dark:bg-surface-dark/20", props.Class) }>
		{ children... }
	</div>
}
