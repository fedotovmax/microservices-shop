package modal

import "github.com/fedotovmax/microservices-shop/customer-site/pkg/utils"

type ModalBodyProps struct {
	Class string
}

templ ModalBody(props *ModalBodyProps) {
	<div class={ utils.TwMerge("px-4 py-8", props.Class) }>
		{ children... }
	</div>
}
